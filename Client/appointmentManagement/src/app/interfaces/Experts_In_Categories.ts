import { Time } from '@angular/common';
import { TimeSpan } from './TimeSpan';

export interface Experts_In_Categories{
    categoryId :number,
    professionId: number,
    cityId:number,
    careLength :number,
    ageBracketId:number,
    expertId:number
}